# Kotlin Standards Compliance Report

Generated by ast-grep scan

## Executive Summary

- **Total unsafe `!!` operators found:** 27
- **Files affected:** 18 files
- **Priority:** CRITICAL - Can cause NullPointerException at runtime

## Files by Priority

### CRITICAL - Core Module (3 occurrences)
These affect core functionality and should be fixed first:

1. `core/src/main/java/my/noveldokusha/core/utils/SavedStateHandleDelegates.kt` - 6 occurrences
2. `core/src/main/java/my/noveldokusha/core/utils/SavedStateHandleComposeDelegates.kt` - 1 occurrence

### HIGH - Scraper Module (14 occurrences)
Web scraping failures if elements not found:

1. `scraper/src/main/java/my/noveldokusha/scraper/sources/Saikai.kt` - 5 occurrences
2. `scraper/src/main/java/my/noveldokusha/scraper/sources/NovelUpdates.kt` - 3 occurrences
3. `scraper/src/main/java/my/noveldokusha/scraper/sources/Reddit.kt` - 2 occurrences
4. `scraper/src/main/java/my/noveldokusha/scraper/sources/AT.kt` - 2 occurrences
5. `scraper/src/main/java/my/noveldokusha/scraper/sources/Sousetsuka.kt` - 1 occurrence

### MEDIUM - Features Module (4 occurrences)
ViewModel and adapter issues:

1. `features/sourceExplorer/src/main/java/my/noveldokusha/sourceexplorer/SourceCatalogViewModel.kt` - 1 occurrence
2. `features/databaseExplorer/src/main/java/my/noveldoksuha/databaseexplorer/databaseSearch/DatabaseSearchViewModel.kt` - 1 occurrence
3. `features/databaseExplorer/src/main/java/my/noveldoksuha/databaseexplorer/databaseBookInfo/DatabaseBookInfoViewModel.kt` - 1 occurrence
4. `features/reader/src/main/java/my/noveldokusha/features/reader/domain/ReaderItemAdapter.kt` - 1 occurrence

### LOW - Utility Files (1 occurrence)
1. `networking/src/main/java/my/noveldokusha/network/Utils.kt` - 1 occurrence

## Recommended Fix Pattern

### For Delegate Classes (SavedStateHandleDelegates.kt)

**Before:**
```kotlin
operator fun getValue(thisRef: Any, property: KProperty<*>): T =
    savedStateHandle[property.name]!!
```

**After:**
```kotlin
operator fun getValue(thisRef: Any, property: KProperty<*>): T =
    savedStateHandle[property.name]
        ?: throw IllegalStateException("Required value '${property.name}' not found in SavedStateHandle")
```

### For ViewModel Sources/Databases

**Before:**
```kotlin
private val source = scraper.getCompatibleSourceCatalog(sourceBaseUrl)!!
```

**After:**
```kotlin
private val source = scraper.getCompatibleSourceCatalog(sourceBaseUrl)
    ?: throw IllegalStateException("Source not found for URL: $sourceBaseUrl")
```

### For Web Scraping (Saikai.kt, NovelUpdates.kt, etc.)

**Before:**
```kotlin
val element = doc.selectFirst(".class")!!
val text = element.text()
```

**After:**
```kotlin
val element = doc.selectFirst(".class") ?: return ""
val text = element.text()
```

Or with exception:
```kotlin
val element = doc.selectFirst(".class") 
    ?: throw IllegalStateException("Required element '.class' not found")
```

## Migration Status

### ✅ Completed (Previous Standards Pass)
- `core/utils/IntentExtraDelegates.kt` - 8 delegates fixed
- `scraper/databases/BakaUpdates.kt` - 5 unsafe !! fixed
- 7 scraper source files fixed (RoyalRoad, BacaLightnovel, etc.)
- `tooling/epub_parser/EpubParser.kt` - ToC access fixed
- `tooling/backup_restore/RestoreDataService.kt` - OkHttp 5.x compat

### ⏳ Remaining (This Report)
- 27 unsafe !! operators across 18 files (see details above)

## Next Steps

1. **Immediate:** Fix SavedStateHandleDelegates.kt (core functionality)
2. **Short-term:** Fix scraper sources (user-facing features)
3. **Medium-term:** Fix ViewModels (error handling improvement)
4. **Low priority:** Utility files (network utils)

## ast-grep Commands Used

```bash
# Find all unsafe !! operators
ast-grep scan --rule ast-grep-rules/unsafe-double-bang.yml

# Find deprecated onBackPressed
ast-grep scan --rule ast-grep-rules/deprecated-onBackPressed.yml

# Apply fixes interactively
ast-grep scan --rule ast-grep-rules/unsafe-double-bang.yml --interactive
```

---

**Generated:** 2025-03-14  
**Tool:** ast-grep  
**Kotlin Version:** 2.1.21
